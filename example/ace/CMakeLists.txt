if(PROTOBUF_FOUND)
    add_subdirectory(logging)
else()
    add_subdirectory(logging EXCLUDE_FROM_ALL)
endif()

if(BOOSTPO_LIBRARY)
    add_subdirectory(ttcp)
else()
    add_subdirectory(ttcp EXCLUDE_FROM_ALL)
endif()
